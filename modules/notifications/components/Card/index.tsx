import { ReactElement } from 'react';
import { INotifCardProps } from './interface';

export default function NotifCard({
  date,
  title,
  description,
  thumb,
  data
}: INotifCardProps): ReactElement {
  return (
    <div>
      
    </div>
  );
}
